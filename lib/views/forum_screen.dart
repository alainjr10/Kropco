import 'package:flutter/material.dart';

class Forum extends StatelessWidget {
  static const forumScreenId = "/forum_screen";
  const Forum({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Forum"),
    );
  }
}
